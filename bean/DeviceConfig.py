class DeviceConfig():
    deviceId = 1
    code = 1
    devicePrefix = 1

    def __init__(self, deviceId,devicePrefix,code):
        self.deviceId = deviceId
        self.code = code
        self.devicePrefix = devicePrefix
